

#include <string>
#include "StfToText.h"

std::string Change2PlainText(const std::string& strFormattedText)
{
	std::string strPlainText;
	CStfToText::Transform(strFormattedText.data(), strFormattedText.size(), strPlainText);
	
	return strPlainText;
}

